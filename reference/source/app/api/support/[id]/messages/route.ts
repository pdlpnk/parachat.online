import { deriveNetworkIdentifier, errorResponse, getSupportNotificationService, json, limitRequest, readJsonBody, requireRequestPrincipal, requireTrustedOrigin } from "@/lib/server";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { requireTrustedOrigin(request); const principal = await requireRequestPrincipal(request); await limitRequest({ namespace: "support.message", key: `${principal.userId}:${deriveNetworkIdentifier(request)}`, limit: 40, windowSeconds: 3600 }); const body = await readJsonBody(request); return json(await getSupportNotificationService().sendMessage(principal, (await params).id, String(body.body ?? ""), String(body.idempotencyKey ?? ""))); } catch (error) { return errorResponse(error); } }
